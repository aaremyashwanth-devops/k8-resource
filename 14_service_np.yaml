apiVersion: v1
kind: service
metadata: 
  name: nodeport
spec:
  type: NodePort
  selector:
    project: roboshop
    environment: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
       
   