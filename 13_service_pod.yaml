apiVersion: v1
kind: Pod
metadata: 
  name: service-pod
  namespace: roboshop
  # labels:
  #   project: roboshop
  #   environment: dev
  #   component: frontend
spec:
  containers:
  - name: almalinux
    image: almalinux
    command: ["sleep","1000"]
