apiVersion: v1
kind: Pod
metadata: 
  name: service-pod
  namespace: roboshop
  # labels:
  #   project: roboshop
  #   environment: dev
  #   component: frontend
spec:
  containers:
  - name: almalinux
    image: almalinux:9
    command: ["sleep","1000"]
