apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: roboshop
spec:
  selector:
    project: roboshop
    environment: dev
    
  ports:
  - protocol: TCP
    prot: 80
    targetPort: 80
