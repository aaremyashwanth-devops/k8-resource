apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: roboshop
spec:
  selector:
    project: roboshop
    environment: dev
    components: frontend
  ports:
  -  protocol: TCP
     prot: 80
     target port: 80
